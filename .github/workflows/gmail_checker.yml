name: Hourly Job Application Tracker

on:
  schedule:
    # Runs at the start of every hour
    - cron: "0 * * * *"
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  check-gmail:
    runs-on: ubuntu-latest
    # Set the default working directory if your files are inside a subfolder
    defaults:
      run:
        working-directory: ./jobapp-tracker

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-2

      - name: Run Gmail Checker
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NTFY_TOPIC_URL: ${{ secrets.NTFY_TOPIC_URL }}
          AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
        run: |
          # Write Gmail token from GitHub Secret to token.json
          python -c 'import os; open("token.json", "w").write(os.environ["GMAIL_TOKEN_JSON"])'
          python main.py

      - name: Update token and tracker in repo
        if: always() # Always run this step to persist refreshed tokens and CSV state
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: Auto-update job alerts and Gmail token"
          file_pattern: "job_alert.csv token.json"
